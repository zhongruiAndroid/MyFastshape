<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.test.fastshape"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".XMLActivity"/>
        <activity android:name=".TestLinearlayoutActivity"/>
        <activity android:name=".test.ShapeTestListActivity">
        </activity>
        <activity android:name=".test.TestFirstActivity">
        </activity>
        <activity android:name=".test.TestSecondActivity">
        </activity>
        <activity android:name=".test.TestThirdActivity">
        </activity>
    </application>

</manifest>